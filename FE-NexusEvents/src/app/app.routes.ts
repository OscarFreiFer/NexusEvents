import { Routes } from '@angular/router';
import { HomeScreenComponent } from './routes/home-screen/home-screen.component';

export const routes: Routes = [{ path: '', component: HomeScreenComponent }];
